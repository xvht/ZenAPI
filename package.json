{
  "name": "zen-data",
  "version": "1.0.0",
  "description": "CRUD Operation HTTP API for CloudFlare R2 Buckets",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "bundle": "bun build ./src/index.ts --outfile=dist/index.js --minify",
    "deploy": "wrangler deploy",
    "secret": "wrangler secret put AUTH_TOKEN"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "hono": "^4.7.0",
    "nanoid": "^5.0.9",
    "wrangler": "3.107.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250204.0",
    "@types/node": "^22.13.1"
  }
}